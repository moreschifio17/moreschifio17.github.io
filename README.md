# moreschifio17.dev

## Notes

- Testimonial avatars are custom initials styled directly in `index.html` so the portfolio does not depend on external placeholder images.
- Testimonial initial avatars should keep equal width and height so badges stay perfectly circular, including two-letter initials like `CE`.
